# CONTEXT — 照妖镜

知乎 + X（Twitter）AI 内容检测 Chrome 扩展（MV3）的领域词汇。只含词汇表，不含实现细节；由 grilling 会话逐步固化。

## Language（术语表）

**判定分数 (verdict score)**:
对一条回答"由人类撰写"的可信度估计，取值 0–100：100 几乎确定人工，0 几乎确定 AI。采用扣分制，由引擎产出，可被用户覆盖。
_Avoid_: AI 置信度、可疑度

**阈值 (threshold)**:
把判定分数映射为离散等级的配置值，默认两档：≤70 疑似 AI，≤40 确定 AI（分数越低越可疑）。阈值可配置，驱动处置动作。
_Avoid_: 分数线、档位线

**处置动作 (disposition)**:
针对某个判定等级采取的界面行为，如标记提醒、折叠、过滤。MVP 只实现"标记"，折叠/过滤为后续迭代。
_Avoid_: 操作、行为

**引擎 (engine)**:
产出判定分数的两级后端：**初审（校准打分）**对 21 类写作特征的命中数 + 4 统计文本特征（句长波动/标点密度/平均句长/逗号占比）做逻辑回归校准（权重由 C-ReD 知乎问答语料 + deepseek-v4-flash 平衡拟合，v4），再把 5 类待拟合中文 humanizer 痕迹（证据越界/假案例/翻译腔/解释腔/说明书结构）按自定义正则同一出口叠在 sigmoid 之后小幅扣分；**二审（云端校验器）**仅在分数落入模糊带（10–55）时介入，调用 OpenAI 兼容接口的远程 LLM（默认 DeepSeek），分析内容将外发至该服务商。本地模型方案已评估后排除。
_Avoid_: 后端、检测器

**校准打分 (calibrated scoring)**:
初审的评分方式：`score = round(σ(Σ w_i·x_i + b) × 100)`，特征 = 21 类写作痕迹的命中数（cap 前）+ 4 统计文本特征（句长波动/标点密度/平均句长/逗号占比），权重/截距由 C-ReD 知乎问答语料 + deepseek-v4-flash 平衡拟合的逻辑回归产出。替代早期手拍扣分制（整数权重、分数无概率语义、AUROC 0.68）。理由面板展示逐特征对数几率贡献（`w_i·x_i`，正 = 偏向人类、负 = 偏向 AI）；权重随数据落地——冒号/句号当顿号/伪亲密等在该域为**人类信号**（旧扣分制对此的惩罚是域偏差）。默认阈值 确定 ≤30 / 疑似 ≤50，模糊带 [10,55]（v4 统计特征压低部分人类长文，下界 10 救回无挽救误判、上界 55 救回 flash 漏判）。
> **v4 补丁（2026-09-03）**：另增 5 类可计数中文痕迹（证据越界、假案例、翻译腔、解释腔/上帝视角、说明书结构）。本轮无 C-ReD+flash jsonl，**不把新维拼进 v4 权重表**；命中后按自定义正则语义在 sigmoid 分数上小幅扣分。说明书结构 `minChars=200`，短于该字数（含 X 默认 40 字）不计。数据齐后用 `eval/pilot/fit-v5-new-traces.js` 联合拟合，不稳/基率近 0 置零。
> **v3（2026-08-29）**：训练集由「人类 2,956 + 9 旧代 LLM 26,115」改为「人类 2,956 + deepseek-v4-flash 2,956」——旧 9 LLM 已在词法特征上人类化（issue 06），`dash-repetition` 在旧域被噪声拟合为人类信号、flash 上反转。flash-domain 重拟合后 flash 漏判 82%→38%，代价是旧 9 LLM 泛化崩（正常 74%），为域内 tradeoff（国内现网主用 flash 类新模型）。置零噪声特征 4 → 5（新增跨种子不稳的 fake-colloquial）。
> **v4（2026-08-29）**：新增 4 统计文本特征（句长波动/标点密度/平均句长/逗号占比，`src/engine/stat-features.js`，纯 JS 字符级无分词依赖），与 21 词法命中数合并为 25 维联合拟合。flash 漏判 38%→21%，旧 AI 泛化改善（74%→45%），学术误报类大幅改善（5→35 疑似）。charEntropy/ttr 因长度混淆弃用。`USE_STAT_FEATURES` 代码级开关。
_Avoid_: 机器学习打分、AI 模型判定

**AI 创作痕迹 (AI trace)**:
初审的输入特征：可列举、可解释的 AI 常见创作特征（措辞套话、工整结构、无个人化细节等）。命中次数构成特征向量，经校准权重加权（方向由数据决定——某些痕迹在该域人类更常用，权重为正即偏向人类）。命中清单用于"点击看理由"。
_Avoid_: AI 特征、扣分项

**输入窗口 (input window)**:
被分析的回答正文片段：全文（折叠长文亦在 DOM 内）与 2000 字上限取较小者（可配置），不含标题，图/引用开头跳至首个文本段；"只看开头一两段"为可选模式。
_Avoid_: 采样片段、文本窗口

**文章判定 (article judgment)**:
对知乎文章（/p/ 详情页与首页/问题页文章列表卡）的判定：与回答判定共享判定阈值与二审权重（全站尺度一致），但设置组独立（默认「头尾各半」抽样，上限 4000 字 = 头尾各 2000，覆盖万字长文结尾套话；字数下限/上限独立）、二审预算维度独立（每页上限各自计数）、覆盖命名空间独立（'p'+文章 ID）。hideAiBody 不作用于文章正文——仅角标提示不藏文。
_Avoid_: 专栏检测

**覆盖 (override)**:
用户手动纠正引擎判定结果的记录，优先级高于引擎判定，以回答 ID 为键，持久化于扩展本地存储（chrome.storage.local）。
_Avoid_: 纠正、手动标记

**作者规则 (author rule)**:
作者级硬规则，两个方向对称：屏蔽 = 该作者全部内容整卡占位、不做任何检测；信任 = 一轮不跑、仅显示灰色小签「已信任」。优先级最高（作者级 > 卡片覆盖 > 引擎判定），检查位于文本提取之前，跨页面持久化于 chrome.storage.local。知乎身份为 `zhihu.com/people/<token>`（存储键即 token）；X 身份为 handle（存储键 `x:<handle>`，小写），**不得**与知乎 people token 落在同一键。匿名/无作者链接不可操作。
_Avoid_: 拉黑、黑名单、白名单

**推文判定 (tweet judgment)**:
对 X / Twitter 推文（`article[data-testid="tweet"]`）的判定：与回答共享引擎、阈值与二审权重，覆盖键 `x:<snowflake>`，作者键 `x:<handle>`，字数下限独立（`xMinChars`，默认 40）。只标顶层推文；嵌套引用与轮播内推文不重复打角标。hideAiBody 作用于推文正文。
_Avoid_: 推特检测

**创作声明 (AI declaration)**:
知乎官方「包含 AI 辅助创作」创作声明（作者在编辑器主动添加，见官方[治理细则](https://zhuanlan.zhihu.com/p/624717941)），挂在卡片时间区 `.ContentItem-time` 内（SSR 首帧即存在，回答卡/文章列表卡/文章详情页通用；外层容器为 hash 类名，只匹配稳定文本）。带声明的内容作者已自认含 AI 辅助——**跳过整条评分管线**（不提取/不跑规则/不进二审/不消费预算/minChars 与正文变化重分析同样跳过），直接渲染「声明 / AI / 作者声明」紫调角标 + 面板引用声明原文。优先级：作者规则 > 手动覆盖 > 创作声明 > 引擎判定；三个判定按钮（认为人工/认为 AI/重新判定）禁用，保留作者级操作。`declaredHideBody`（默认 false）控制是否折叠正文——已自认 AI 不主动藏文。声明翻转（作者后续添加/移除）由全卡快照对比自动重扫。
_Avoid_: 披露、标注、AIGC 标签

**触发 (trigger)**:
分析发生的时机：页面加载后分析已渲染回答，滚动时分析新出现回答，并提供手动"重新分析"入口。
_Avoid_: 时机、调度
